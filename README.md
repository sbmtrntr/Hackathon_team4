## GitHubの使い方

```
# 最初だけ
git init
git branch -M main
git remote add origin https://github.com/sbmtrntr/Hackathon_team4.git

# ローカル（手元）変更をリポジトリに反映させる
git add ファイル名
git commit -m "やったこと"
git push -u origin main

## リポジトリの変更をローカルに持ってくる
git pull origin main
```